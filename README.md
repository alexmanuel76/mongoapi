Small java API that save data in a mongoDB each time is called the /add method.

generic execution via command:
java -jar /your-path/lab-0.0.1-SNAPSHOT.jar --MONGODB_URI="mongodb://your-server:27017/balancertest"
